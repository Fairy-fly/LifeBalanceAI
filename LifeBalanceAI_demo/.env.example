# Local development example. Copy to .env locally if needed.
# Never commit real keys.
# Android debug APKs do not bundle .env. For device tests, provide keys via
# environment/local app data outside the repository, then restart the app.
DEEPSEEK_API_KEY=
OPENAI_API_KEY=
LIFEBALANCE_THEME=light
